{
  "title": "Apple App Store Scraper",
  "description": "Search the Apple App Store or look up apps by ID to extract app details, ratings, prices, and metadata. No login or API key required.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchTerms": {
      "title": "Search Terms",
      "description": "Keywords to search the App Store, e.g. \"weather\", \"photo editor\".",
      "type": "array",
      "editor": "stringList",
      "prefill": ["chatgpt"],
      "default": []
    },
    "appIds": {
      "title": "App IDs",
      "description": "Specific App Store app IDs to look up (the numeric trackId, e.g. 284882215). Comma lists are batched.",
      "type": "array",
      "editor": "stringList",
      "prefill": ["6448311069"],
      "default": ["6448311069"]
    },
    "entity": {
      "title": "App Type",
      "description": "Which kind of apps to return.",
      "type": "string",
      "editor": "select",
      "default": "software",
      "enum": ["software", "iPadSoftware", "macSoftware"],
      "enumTitles": ["iPhone Apps", "iPad Apps", "Mac Apps"]
    },
    "country": {
      "title": "Country / Storefront",
      "description": "Two-letter App Store country code (e.g. \"us\", \"gb\", \"in\").",
      "type": "string",
      "editor": "textfield",
      "default": "us"
    },
    "maxResultsPerTerm": {
      "title": "Max Results Per Search Term",
      "description": "Maximum apps to return per search term. Use 1-5 for a quick test. The App Store API caps search at 200.",
      "type": "integer",
      "editor": "number",
      "default": 1,
      "minimum": 1,
      "maximum": 200
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "description": "Proxy settings. The iTunes API is generally open; enable proxies to avoid rate limits on large runs.",
      "type": "object",
      "editor": "proxy",
      "default": { "useApifyProxy": false }
    }
  }
}
